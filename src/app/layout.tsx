import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Interactive Wall Calendar',
  description:
    'A polished, interactive wall calendar component with date range selection, notes, and responsive design.',
  keywords: ['calendar', 'wall calendar', 'date picker', 'range selector', 'react'],
  authors: [{ name: 'Irfan' }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>{children}</body>
    </html>
  );
}
